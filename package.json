{
  "name": "artisan-template",
  "version": "1.0.0",
  "description": "Template site vitrine statique pour artisan local — plomberie, chauffage, climatisation, rénovation",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "python3 -m http.server 8000 -d dist"
  }
}
