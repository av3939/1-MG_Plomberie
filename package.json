{
  "name": "bp2c",
  "version": "1.0.0",
  "description": "BP2C — Site statique plomberie, chauffage, climatisation, rénovation (Fréjus)",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "python3 -m http.server 8000 -d dist"
  }
}
